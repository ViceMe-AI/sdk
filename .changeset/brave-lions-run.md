---
'@viceme-ai/sdk': minor
---

B0.1 foundation: headless core (`createViceMe`), public error model, lifecycle state machine, fetch transport, work-session manager, `@viceme-ai/sdk/testing` mock adapter (`createTestViceMe` + `createMemoryTransport`), CDN auto-loader (IIFE) with `data-viceme-*` parsing, manifest-driven chunk loading, registry dedup, `viceme:*` DOM events and Shadow DOM mount contract; ESM + IIFE + types + manifest builds, tarball audit, public-surface and size-budget gates.
