---
'@viceme-ai/sdk': patch
---

B0.3: release & CDN automation — npm OIDC trusted publishing workflow with provenance and release-artifact attachment (Changesets flow), Promote CDN workflow (local digest re-verification, immutable exact-version upload, public read-back, explicit stable-alias move, rollback = re-point alias), `verify-cdn.mjs` read-back tool (sha256/SRI/bytes/content-type/cache headers, alias expectation) with tests, and `docs/RELEASE.md` runbook.
